<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aldana.jesus.thedot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheDot">
        <activity android:name=".information"></activity>
        <activity android:name=".mydays" />
        <activity android:name=".howareyoutoday" />
        <activity android:name=".intentions_day" />
        <activity android:name=".diary" />
        <activity android:name=".afirmations" />
        <activity android:name=".configuration" />
        <activity android:name=".journal" />
        <activity android:name=".profile" />
        <activity android:name=".methods" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>